/* 

=> Go is a statically typed language which means type are determined at compile time rather than runtime .

   Compiler check correctness before program excutes , helping catch errors early and improving performance

=> Go is a compiled language where the source code is compiled into a native executable binary before runtime. 
   
   The binary can run directly on the operating system without needing an interpreter or virtual machine.   





*/